# Site settings
title: WFP UI Guide
email: ""
description: ""
baseurl: "" # "/content/wfp/guide" # the subpath of your site, e.g. /blog
url: "" # "http://matthewmorek.com" # the base hostname & protocol for your site

exclude: ["*.sublime-*", "*.sh", "*.yml", "*.DS_*"]

# Build settings
markdown: kramdown
highlighter: pygments

kramdown:
  input: GFM

category-list: [General, Components, Patterns, Info]

# Defaults
defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: "default"